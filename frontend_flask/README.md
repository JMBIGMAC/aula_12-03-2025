# Instruções para rodar o frontend Flask

1. Instale as dependências:
   pip install flask requests

2. Execute o servidor Flask:
   python app.py

3. Acesse http://localhost:5000 em seu navegador.

O Flask servirá a página Home e buscará dados do backend Django via JSON.
